<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="12dp">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="My Car Tracker — نمایشگر (Viewer)"
        android:textSize="18sp"
        android:textStyle="bold"
        android:paddingBottom="4dp" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="آخرین پیامک دریافتی (خام، برای عیب‌یابی):"
        android:textSize="11sp"
        android:textColor="#999999" />

    <TextView
        android:id="@+id/debugLastSmsText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="—"
        android:textSize="11sp"
        android:textColor="#999999"
        android:paddingBottom="8dp" />

    <!-- Part 1: raw message history -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textStyle="bold"
        android:text="پیامک‌های دریافتی از ردیاب" />

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginTop="4dp"
        android:background="#F5F5F5">

        <ListView
            android:id="@+id/historyListView"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />

        <TextView
            android:id="@+id/emptyHistoryText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:text="هنوز پیامکی دریافت نشده."
            android:textColor="#888888" />

    </FrameLayout>

    <!-- Part 2: CSV-style table of parsed coordinates -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textStyle="bold"
        android:layout_marginTop="12dp"
        android:text="مختصات ثبت‌شده (CSV)" />

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginTop="4dp"
        android:background="#F5F5F5">

        <ListView
            android:id="@+id/csvListView"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />

        <TextView
            android:id="@+id/emptyCsvText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:text="هنوز مختصاتی ثبت نشده."
            android:textColor="#888888" />

    </FrameLayout>

    <!-- Part 3: single button leading to all settings/commands -->
    <Button
        android:id="@+id/openCommandsButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:text="تنظیمات و ارسال فرامین" />

</LinearLayout>
